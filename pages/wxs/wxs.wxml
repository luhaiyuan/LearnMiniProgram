<!-- 1.错误的写法 -->
<!-- <text>{{25.66666.toFixed(2)}}</text> -->

<!-- 
  注意: wxml中是不能直接调用Page/Component中定义的函数的，所以下面写法也是错误的
  <text>{{ numberTofixed(25.66666) }}</text>
 -->

<!-- 2.wxs的定义方式: -->
<!-- 2.1直接在wxml中定义 -->
<!-- <wxs module="info">
  var message = 'hello world';
  var name = 'coderwhy';
  function sum(num1, num2) {
    return num1 + num2;
  }

  module.exports = {
    message: message,
    name: name,
    sum: sum
  }
</wxs> -->

<!-- 2.2 定义在单独的wxs文件中，再使用<wxs>标签进行导入
    注意: 不能使用绝对路径，必须使用相对路径
 -->
<wxs src="../../wsx/info.wxs" module="info"></wxs>
<view>{{info.message}}</view>
<view>{{info.name}}</view>
<view>{{info.sum(20, 30)}}</view>

<!-- 3.wxs的应用 -->
<view>---------------wxs的应用--------------</view>
<wxs src="../../wsx/format.wxs" module="format"></wxs>
<view>{{ format.priceFormat(price, 3) }}</view>

<view>{{ format.dateFormat(time) }}</view>